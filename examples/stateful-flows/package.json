{
	"name": "@mcesystems/reflow-example-stateful-flows",
	"private": true,
	"version": "4.0.1",
	"description": "A ui-less flow that shows how flows can retain state between invocations",
	"main": "dist/index",
	"scripts": {
		"build": "tsc"
	},
	"author": "mceSystems",
	"dependencies": {
		"@mcesystems/reflow": "^4.0.1"
	},
	"devDependencies": {
		"typescript": "^3.5.3"
	}
}
